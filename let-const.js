let balance = 1200;
balance = 32332;
const lifePartner = 'janPakhi';
const weTogether = 'shahin' + lifePartner;
console.log(weTogether);
// lifePartner = 'moynapakhi'; \\ not alowed. because of const. You need to use let
//array
let numbers = [23.343, 38, 8, 76, 34, 43];
numbers = [23, 43, 4];
console.log(numbers);
//loop
for (let i = 0; i < numbers.length; i++) {
    const number = numbers[i];
}








//object
const student = { roll: 10, name: 'shahin', job: "intern" };
student.name = 'mahmodul';
// student = { job: 'jobless' }\\ not alowed. Because of const. You need to use let
console.log(student);



